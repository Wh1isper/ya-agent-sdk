[project]
name = "yaacli"
dynamic = ["version", "dependencies"]
description = "TUI reference implementation for ya-agent-sdk"
authors = [{ name = "wh1isper", email = "jizhongsheng957@gmail.com" }]
readme = "README.md"
keywords = ["python", "cli", "tui", "ai-agent"]
requires-python = ">=3.11,<3.14"
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Environment :: Console",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Repository = "https://github.com/wh1isper/ya-mono"

[project.scripts]
yaacli = "yaacli.cli:cli"

[project.optional-dependencies]
rs = ["ya-agent-sdk[all,rs]"]

[build-system]
requires = ["hatchling", "uv-dynamic-versioning>=0.7.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
vcs = "git"
style = "pep440"
bump = true

[tool.hatch.metadata.hooks.uv-dynamic-versioning]
dependencies = [
    "ya-agent-sdk[all]=={{ version }}",
    "ya-agent-stream-protocol=={{ version }}",
    "ya-oauth=={{ version }}",
    "ya-oauth-provider=={{ version }}",
    "click>=8.0",
    "pillow>=10.0.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pydantic-ai>=1.100.0,<2",
    "pyobjc-framework-Cocoa>=10.0; platform_system == 'Darwin'",
    "python-dotenv>=1.0.0",
    "ya-agent-environment=={{ version }}",
]

[tool.hatch.build.targets.wheel]
packages = ["yaacli"]

[tool.uv.sources]
ya-agent-environment = { workspace = true }
ya-agent-sdk = { workspace = true }
ya-agent-stream-protocol = { workspace = true }
ya-oauth = { workspace = true }
ya-oauth-provider = { workspace = true }

[tool.deptry]
ignore = ["DEP001"]
package_module_name_map = { "click" = "click", "pillow" = "PIL", "pydantic" = "pydantic", "pydantic-ai" = "pydantic_ai", "pydantic-settings" = "pydantic_settings", "pyobjc-framework-Cocoa" = "AppKit|Foundation", "python-dotenv" = "dotenv", "ya-agent-sdk" = "ya_agent_sdk", "ya-agent-stream-protocol" = "ya_agent_stream_protocol", "ya-agent-environment" = "ya_agent_environment", "ya-oauth" = "ya_oauth", "ya-oauth-provider" = "ya_oauth_provider" }
per_rule_ignores = { DEP001 = ["quick_validate"], DEP003 = ["anyio", "click", "dotenv", "prompt_toolkit", "pillow", "pydantic", "pydantic_ai", "pydantic_settings", "pyobjc-framework-Cocoa", "rich", "yaml", "ya_agent_environment", "ya_agent_sdk", "ya_agent_stream_protocol", "yaacli"], DEP004 = ["pytest"] }
