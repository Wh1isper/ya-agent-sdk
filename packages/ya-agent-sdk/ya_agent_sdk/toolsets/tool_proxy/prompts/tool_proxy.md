External toolsets may be available through proxy tools.
Use `search_tools` when the external tool name or schema is unknown; use `call_tool` only when both are already known.
If validation fails, retry from the returned schema instead of guessing arguments.
