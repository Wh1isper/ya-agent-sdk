<read-media-tool>
<best-practices>
- Use direct YouTube URLs when the active model supports them.
- Provide focused media `instructions` for OCR, UI inspection, transcription, timestamped summary, speaker identification, or specific detail extraction.
- If media is unsupported or too large, use `download`, reduce it with local tools when needed, then use `view` on the local file with focused instructions.
</best-practices>
</read-media-tool>
