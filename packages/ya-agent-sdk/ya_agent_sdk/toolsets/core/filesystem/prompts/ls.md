<ls-tool>
<best-practices>
- Use for shallow directory inspection; use glob for recursive file discovery and grep for content search.
- Filter noisy directories before listing large paths.
- Treat unlimited results as deliberate only after narrowing scope.
- If output is redirected to a saved result file, inspect that file only when the full listing is needed.
</best-practices>
</ls-tool>
