<shell-tool>
Check `<shell-environment>` for the active shell dialect before relying on shell-specific syntax.
Use background mode for long-running commands, servers, builds, and test suites; wait only when the result is needed before proceeding.
Use the process-management tools for background stdin, status, signals, and cleanup.
Prefer filesystem/search tools for ordinary file reading, listing, and searching.
Use `cwd` instead of `cd`.
</shell-tool>
